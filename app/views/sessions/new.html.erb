<%= form_for :session, url: sessions_path do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :password %>
	<%= f.password_field :password %>
	<div class="rem">
		<%= f.label :remember_me %>
		<%= f.check_box :remember_me %>
	</div>
	<%= f.submit %>

<% end %>